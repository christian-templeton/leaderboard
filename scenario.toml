[green_agent]
# Contempletiva agent ID
agentbeats_id = "019bfe70-eb34-7651-9bcd-f58389d104c5"
env = { GOOGLE_API_KEY = "${GOOGLE_API_KEY}", LOG_LEVEL = "INFO" }

[[participants]]
# ACTION REQUIRED: You must register your Activa agent on https://agentbeats.dev
# and replace the placeholder below with your real agentbeats_id.
# The GitHub Action will fail until a valid ID is provided.
agentbeats_id = "REPLACE_WITH_YOUR_ACTIVA_AGENT_ID"
name = "Activa"
env = {}

[config]
# Debate/Assessment configuration
domain = "retail"
num_rounds = 2
